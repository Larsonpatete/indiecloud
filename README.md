# indiecloud
self hosting a cloud service

# Create .env
```
SA_PASSWORD=
pathToFolder=/path/to/data
```
Make sure you run these commands

```bash
docker exec indie-api id -g

sudo chown -R <id>:<id> /path/to/data
```

